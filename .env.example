# Feature flag for the shop section (default off until ready)
PUBLIC_SHOP_ENABLED=false

# Path to the external source-photo library that the admin UI reads from.
# Use forward slashes even on Windows.
ADMIN_SOURCE_ROOT=D:/SynologyDrive/02_靜謐花間_0A/02_作品照片區

# Contact-sheet thumbnails now live inside this project at public/_contact_sheets/
# (no env var needed — path is derived from project root).
